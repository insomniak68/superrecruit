apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: superrecruit-data
  namespace: superrecruit
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
